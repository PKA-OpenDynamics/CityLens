# CityLens Mobile Web App - Production Environment Variables
# This file is for reference only. Set these variables in Netlify Dashboard.

# IMPORTANT: Update these URLs with your CURRENT Cloudflare Tunnel URL
# Cloudflare free tunnels expire and get new URLs each time you restart

# Backend API Base URL (must include /api/v1 or /api/v1/app depending on endpoints)
EXPO_PUBLIC_API_BASE_URL=https://YOUR_TUNNEL_HERE.trycloudflare.com/api/v1

# TomTom API Key (get from https://developer.tomtom.com/)
TOMTOM_API_KEY=your_actual_tomtom_api_key

# Weather API Base URL (base URL only, no /api/v1)
EXPO_PUBLIC_WEATHER_API_BASE_URL=https://YOUR_TUNNEL_HERE.trycloudflare.com

# Reports API Base URL (include /api/v1/app)
EXPO_PUBLIC_REPORTS_API_BASE_URL=https://YOUR_TUNNEL_HERE.trycloudflare.com/api/v1/app

# Auth API Base URL (include /api/v1/app)
EXPO_PUBLIC_AUTH_API_BASE_URL=https://YOUR_TUNNEL_HERE.trycloudflare.com/api/v1/app

# MongoDB Connection (usually not needed in frontend)
# MONGODB_URI=your_mongodb_atlas_uri
# MONGODB_DB_NAME=citylens

# Note: For production, set these variables in Netlify Dashboard:
# Site Settings → Environment Variables
